defmodule CritWeb.Templates.Purpose do

  # See `mix new_purpose`
  def create_a_password_without_needing_an_existing_one, 
    do: "create a password without needing an existing one"

  def form_for_creating_new_user, 
    do: "form for creating new user"

end
