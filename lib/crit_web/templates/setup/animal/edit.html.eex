<h1>Edit Animal</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.setup_animal_path(@conn, :update, @animal)) %>

<span><%= link "Back", to: Routes.setup_animal_path(@conn, :index) %></span>
