<!-- Purpose: <%= Purpose.reservation_by_dates %> -->

<div class="ui main center aligned text container">
  <%= date_or_dates_header(@first_date, @last_date) %> 
  <%= for r <- @reservations do %>
    <%= render "_show_one_reservation.html",
      Map.put(assigns, :reservation, r) %>
  <% end %>
</div>
