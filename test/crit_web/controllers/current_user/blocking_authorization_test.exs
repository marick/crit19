defmodule CritWeb.CurrentUser.BlockingAuthorizationTest do
  use CritWeb.ConnCase
  # alias CritWeb.CurrentUser.Settings.SettingsController
  # alias CritWeb.CurrentUser.Settings.SessionController

  # Everything so far is prior to the need for authorization
end

