defmodule Crit.Usables.AnimalImpl.UpdateUpdateServiceGapTest do
  use Crit.DataCase
  # alias Crit.Usables.AnimalApi
  # alias Crit.Exemplars
  # alias Crit.Extras.AnimalT

  describe "updating a service gap" do
    @tag :skip
    test "" do
    end
  end
end
